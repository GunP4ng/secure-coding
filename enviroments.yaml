name: secure_coding
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - flask 
    - flask-socketio
    - flask-sqlalchemy
    - flask-session
    - flask-login
    - eventlet
    - pillow
    - flask-bcrypt
    - flask-wtf